---
description: 本仓库按 new-website 约定开发，不要另选栈
alwaysApply: true
---

本仓库由 `~/.cursor/skills/new-website` 脚手架生成。技术栈、架构、端口以开发者本机 `defaults.md` 和本仓库 `AGENTS.md` 为准。

- 业务、鉴权、数据库只写在 `apps/api`（Nest）
- `apps/web` / `apps/admin` 只渲染并经 `/api` rewrite 调 Nest
- 不要把 B 端塞进 web 路由分组，不要用 Next Route Handler 当主 API
- 不要改用 3000 / 5432
- Lint / 格式化只有 Biome，全仓一份根 `biome.jsonc`。不要装 ESLint / Prettier，不要在 app 里加 `eslint.config.*` 或 `.prettierrc`。`pnpm lint` 只读，`pnpm format` 才改文件
- 组件只从 `@repo/ui/components/x` 引，不要 `@/components/ui/x`；`apps/*/src/components/ui/` 不该存在。装组件在 app 里跑 `shadcn add`，原语会自动落 `packages/ui`
- 公共 token 在 `@repo/ui/globals.css`，app 的 `globals.css` 只 `@import` 它 + 覆盖 `--primary`。别删 `@source` 和 `transpilePackages: ["@repo/ui"]`
- 滚动条一律 OverlayScrollbars：页面挂 `body`，区域内用 `@repo/ui/components/scroll-area`。不要原生条、`::-webkit-scrollbar` 或 Radix scroll-area
- Tailwind v4 按钮默认箭头。可点的 `button` / `[role="button"]` 用手势，靠 `globals.css` 的 pointer 规则，不要漏补
- 不上 Web 字体。西文 system-ui，汉字走系统简体（苹方 / 微软雅黑）。不要 `next/font` 拉 Geist
- 深色模式跟系统。C 端不放切换器；B 端顶栏 `ThemeModeToggle`（⌘⇧D）。不要上多色板或字体切换
- 样式用 CSS 变量（`cssVariables: true`），写 `bg-primary` 等 token class。不要用 `bg-zinc-950 dark:bg-zinc-50` 这种 className 上色
- 组合件根节点加 `data-slot`。父级改子件用 `data-[slot=…]`，不要靠 class 名或 DOM 路径。不要用它换主题或存状态
- `html lang="zh-CN"`。会话页动态；营销数据才 `use cache`。C 端 `next/image` + metadata。尊重减少动效
- C 端 PWA：`manifest.ts` + icon，可安装。默认不上 Service Worker / Web Push。admin 不做
- 生产：用户在大陆。Compose + nginx + 阿里云 DNS-01。源站可在国内或海外。不要 Cloudflare 代签，不要默认拆 Vercel。应用端口和 Postgres 不对外
- C 端：底贴边。文章 65ch，营销带 1440，工具/搜索/列表用 `page-shell-wide`（1920）。不要整页套 `page-prose`
- B 端：列表页主体是表格，新增走 Dialog / Sheet 不常驻列表上方。每块一张 Card，页面自己写 h1。下拉 `Select` 不用原生 `<select>`，表格 `Table` 不用裸 `<table>`，加载 `Skeleton` 不用文本。三态必写，照 `app/users/page.tsx` 长。顶栏竖线 `Separator` 必须带 `data-[orientation=vertical]:h-4` 和 `self-center`，不要只写 `h-4`
- B 端外壳提到 `app/layout.tsx`，别丢 `SidebarProvider` 的内联 `style`（`--sidebar-width` / `--header-height`）。`site-header` 两层 `items-center` 不要动。竖线 `Separator` 写 `data-[orientation=vertical]:h-4` **和** `self-center`（Base UI 再加 `data-vertical:*`）
- 侧栏跟 URL 高亮：`nav-main.tsx` 用 `usePathname` + `Link` + `isActive`，子路由算父项命中。加菜单只改 `app-sidebar.tsx` 的 `data`
- 侧栏骨架别拆：品牌区 / 主导航 / 次级区 / 底部 `NavUser` 都要留，只换 `app-sidebar.tsx` 里的 `data`。`nav-user.tsx` 已接 `authClient.useSession()` + `signOut()`
- 按钮尺寸：页头主操作用默认 size（h-9），卡片内工具栏和表格行内用 `sm`（h-8），行内图标 `icon-sm`。同一行尺寸要一致
- 按钮视觉用 shadcn `default`/`sm`。44px 是热区不是外观；不要把列表 CTA 撑满行高
- 刷新不丢：筛选/搜索/分页用 `nuqs` 进 URL；侧栏开合等偏好用 cookie/localStorage。不要只放 `useState`
- 登录：本地 `pnpm db:seed` 出员工和会员，不要靠手注册。会员 `user@example.com` 只登 C 端，员工 `admin@example.com` 只登 B 端。Cookie 不共享。会话 7 天 + `rememberMe`。生产禁止 seed，后台不开放注册
