# 网盘妹 · 全网搜索技术方案

状态：方向已定，未实施。  
日期：2026-09。  
上游引擎：[fish2018/pansou](https://github.com/fish2018/pansou)（MIT）。  
运行镜像：`ghcr.io/fish2018/pansou`（官方 API，不要 `pansou-web`）。

## 1. 结论

本站**不收录、不录入、不经营资源库**。搜索结果来自公开网络上的分享链接（Telegram 频道 + 第三方搜盘插件），点结果跳原网盘。本站不存文件。

Pansou 是**唯一检索引擎**。Nest 只做门面：鉴权、限流、字段对齐、错误形态。浏览器和 Next **不直连** Pansou。

运行镜像固定为作者官方 API 包：**`ghcr.io/fish2018/pansou`**（容器内 `:8888`）。不要用捆了前端的 `ghcr.io/fish2018/pansou-web`。不自建、不从源码打另一份默认镜像。生产锁 tag 或 digest，不跟 `latest` 盲升。

## 2. 目标与非目标

**做**

- C 端一个搜索框，按关键词 / 网盘类型出结果，点「打开网盘」进原盘。
- 海外源站（优先港 / 新 / 东京）跑 Pansou，由服务器出网拉 TG 和插件站；大陆用户只打本站 HTTPS。
- 对外接口仍是现在的 `GET /api/resources/search`，经 web `/api` rewrite 进 Nest。

**不做**

- 后台录入资源、用户投稿、自建资源表当搜索源。
- 浏览器直连 Pansou、把 Pansou 端口映射到公网。
- 把 PanHub 或其它 Nuxt 整站嵌进来当引擎。
- 把业务写进 Next Route Handler / Server Actions。
- 默认上 Vercel / Cloudflare 代签（部署仍见 `DEPLOY.md`）。

## 3. 架构

```
大陆访客
  → nginx:443（www / admin）
  → web:3501 / admin:3502
  → /api rewrite
  → Nest:3500
       ├─ Better Auth、用户、限流、日志
       └─ 内网 HTTP → Pansou:8888 /api/search
                         ├─ Telegram 公开频道
                         └─ 插件（其它搜盘站）
       ← 映射成 C 端列表结构
  → 页面渲染；外链用 buttonVariants 打在 <a> 上
```

| 进程 | 开发端口 | 生产 | 谁能访问 |
|---|---|---|---|
| web | 3501 | 网关反代，不对外暴露应用端口 | 访客 |
| admin | 3502 | 同上 | 员工 |
| Nest | 3500 | compose 内网 `api:3000` | 仅 web / admin rewrite |
| Pansou | 本机可绑 `3588→8888` 方便调试 | **只听内网，不映射 80/443 以外的任何口** | **仅 Nest** |
| Postgres | 3532 | 不发布 5432 | 仅 Nest（会员 / 员工，**不存搜索结果**） |

Pansou 开发端口用 `35` 段的 `3588`，不要占用 `8888` 宿主机，也不要用 `3000` / `5432`。

## 4. 请求路径

1. 用户在 C 端提交或改 URL（`nuqs`：`q`、`provider`）。
2. `search-home.tsx` 继续 `fetch('/api/resources/search?…', { credentials: 'same-origin' })`。
3. Nest `ResourcesController.search` 校验 `@repo/shared` 的 query（`q` ≤ 80、`provider`、分页）。
4. Nest 调 Pansou（超时建议 8～12 秒，短超时先于 Pansou 自己的 30 秒后台补全；第一期不做流式）。
5. 把 Pansou 的 `merged_by_type` / `results` 收成现有 `ResourceSearchResponse`：`{ items, total, page, pageSize }`。
6. Pansou 挂了或超时：HTTP 502/504 + 统一错误文案，C 端走已有错态。不要回退到库里的演示数据。

空关键词：第一期约定为「不打 Pansou、返回空列表」，避免无词扫全网。首页热门以后另做，不靠空搜。

## 5. 字段对齐

Pansou 一条结果大致是：标题、正文、`links[]`（`type` / `url` / `password`）、时间、频道。一条里可能有多个网盘链接，要**拆成多条** C 端卡片，或一卡多链。第一期：**一链一张卡**，同一标题重复出现可以接受，后续再按 URL 去重。

| C 端 `Resource`（现状） | 来自 Pansou |
|---|---|
| `id` | 稳定合成：`sha256(url)` 前 32 位或 `unique_id + url`，不要随机 UUID（刷新列表不能跳 id） |
| `title` | `title`，空则截断 `content` |
| `description` | `content` 截断到 500 |
| `provider` | `link.type` 映射到 `diskProviderSchema` |
| `shareUrl` | `link.url`（必须 http(s)） |
| `shareCode` | `link.password` |
| `tags` | 频道名或空 |
| `published` | 恒为 `true`（聚合结果不进库） |
| `createdAt` / `updatedAt` | `datetime`，解析失败用当前请求时间 |

网盘类型映射（Pansou → 本站）：

| Pansou | 本站 |
|---|---|
| `baidu` | `baidu` |
| `aliyun` | `aliyun` |
| `quark` | `quark` |
| `tianyi` | `tianyi` |
| `xunlei` | `xunlei` |
| `weiyun` | `weiyun` |
| `115` / `uc` / `mobile` / `123` / `pikpak` / `guangya` | 第一期进 `other`，标签里保留原名 |
| `lanzou` | `lanzou`（若上游有） |
| `magnet` / `ed2k` | **丢弃**（产品只做网盘分享页） |

`provider` 筛选：Nest 把本站枚举译回 Pansou 的 `cloud_types` 再问上游；`other` 时不要传 `cloud_types`，回来再过滤。

分页：Pansou 一次返回一坨。Nest 内存切片：`page` / `pageSize`（现默认 12、最大 50）。`total` 用映射后的条数。第一期不做上游翻页。

## 6. Nest 怎么调

新增环境变量（启动时 Zod 校验，未声明不读）：

| 变量 | 含义 | 开发示例 |
|---|---|---|
| `PANSOU_URL` | 内网基址，**不要** `NEXT_PUBLIC_` | `http://127.0.0.1:3588` |
| `PANSOU_TIMEOUT_MS` | 整次等待 | `10000` |
| `PANSOU_CHANNELS` | 可选，覆盖上游默认频道 | 空 = 用镜像默认 |
| `PANSOU_PLUGINS` | 可选，启用插件白名单 | 空 = 用 compose 里那份 |

调用约定：

- `POST {PANSOU_URL}/api/search`，JSON：`{ kw, src: "all", res: "merge", cloud_types?, channels?, plugins? }`。
- 不把用户 Cookie、Authorization 转给 Pansou。
- 服务端打日志：关键词哈希或截断、耗时、上游状态码、条数。不要把完整分享链打进生产日志。
- 同一 `q + provider` 可在 Nest 侧做短 TTL 内存缓存（例如 60 秒），减轻重复打上游。第一期可以不做。

健康检查：Nest `/api/health` 可附带 `pansou: ok|down`（探 `PANSOU_URL/api/health`），生产探针仍以 Nest 为准。

## 7. 进程与 Compose

镜像一律：

```text
ghcr.io/fish2018/pansou:<锁定的 tag 或 digest>
```

开发、生产同一仓库，只改 tag。不要 `pansou-web`，不要 Docker Hub 第三方搬运当默认源。海外机器走 GHCR 拉镜像（与 `DEPLOY.md` 一致）。

**开发**

- 新增 `pnpm pansou:up`：`ghcr.io/fish2018/pansou`，宿主机 `3588→8888`。
- `pnpm dev` 仍起 web / admin / Nest；Nest 读 `PANSOU_URL`。
- Postgres 继续给 Better Auth 用，与搜索结果无关。

**生产**（`infra/docker`，与 `DEPLOY.md` 同一套网关）

- `docker-compose.prod.yml` 增加 `pansou` 服务：`image: ghcr.io/fish2018/pansou:<tag>`，`expose: 8888`，**不要** `ports`。
- Nest 环境：`PANSOU_URL=http://pansou:8888`。
- 网关只反代 web / admin / api。路径 `/pansou`、宿主机 8888 都不开。
- 给 Pansou 一块 cache volume（上游二级缓存）。
- 频道 / 插件用环境变量写死在 compose，不要运行时去拉别人的远程频道表。

海外机器出网访问 `t.me` 和插件站；一般不必再套一层 SOCKS。若某机房出不了 TG，再加 `PROXY`，不是默认。

## 8. 对现有代码的处置

落地搜索转发之后，按序拆掉「本站库当搜索源」：

| 现状 | 处置 |
|---|---|
| `ResourcesService.search` 查 `resource` 表 | 改为调 Pansou + 映射 |
| `onModuleInit` 写入演示资源 | 删除 |
| 后台 `/resources` 录入 / 删除 | 不做新功能；实现搜索后删除该菜单和接口 |
| `createResource` / `updateResource` API | 随后台页一起下线 |
| Drizzle `resource` 表 | 可留空表一个版本，再出迁移删除 |
| C 端 `search-home`、`nuqs`、卡片、外链 `buttonVariants` | 保留；文案去掉「已收录」 |
| `AGENTS.md` 首段「搜索已收录的网盘分享」 | 改成「聚合公开分享，点结果跳原盘」 |

B 端骨架（侧栏、`NavUser`、用户表）留下。员工只做账号，不做资源运营。

## 9. 分期

**一期（可上线）**

- Compose 起 Pansou；Nest 转发 + 映射 + 丢弃磁力；C 端列表能出真链。
- 空词不搜；错态 / 空态；限流（登录/注册已有的那套可扩到 search）。
- 本机：`pnpm pansou:up && pnpm dev`，用「线性代数」等词验收。

**二期**

- URL 去重、同一标题折叠多盘、`other` 拆出 115 / UC 等正式枚举。
- Nest 短缓存；可选「先回部分结果」（要改 C 端，不作为一期）。

**明确不做（除非另开需求）**

- 探活、转存、解析网盘目录树。
- 自建索引库、投稿审核。
- 把 Pansou 源码 vendoring 进 monorepo 当主 API。
- 使用 `ghcr.io/fish2018/pansou-web` 或其它带页面的集成镜像。

## 10. 风险

- 插件站改版或封 IP，结果变空或变少。应对：插件白名单、健康检查、可热改环境变量重启。
- 死链、重复、侵权链接会出现在本站品牌下。本站不存文件，但仍是聚合展示；下架走源头，本站只能过滤关键词 / 域名黑名单（一期可以先不做，文档里留口）。
- Pansou 核心代码测试弱。我们不改它的内部，只锁镜像 tag，不跟 `latest` 盲升。
- 作者 README 有「学习研究、勿用于盈利」的表述；许可证是 MIT。产品决策方自行判断，本文只定技术边界。

## 11. 验收

- 浏览器只出现 `localhost:3501` / 生产 www，Network 里没有 `:8888` / `:3588`。
- 未起 Pansou 时，搜索是明确失败，不是演示种子数据。
- 带 `provider=baidu` 时，卡片 `provider` 只有百度（或映射后的 baidu）。
- 结果里没有 `magnet:` / `ed2k:`。
- `pnpm check` 仍过。
- 生产 compose：`docker ps` 看不到 Pansou 的宿主机端口。
